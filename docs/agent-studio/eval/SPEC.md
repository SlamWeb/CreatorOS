# 第一批运营任务评测

## 第二批：多轮与拒绝错误执行（2026-09-11）

- 新增三条开发集：同会话撤选改标题；不同账号的同名栏目澄清；首轮读取后改变受众，次轮要求选择旧候选。
- 最后一轮单独计工具/结果范围，不能以首轮成功预览替代第二轮成功。过期案例要求重查状态并不尝试创建预览；不以服务端拒绝错误调用冒充 Agent 正确判断。
- 澄清/过期案例的回答语义需人工查看 `final_answer`，自动分数只代表工具与状态约束通过。仍为开发集，不宣称独立测试集或泛化指标。
- `--group advanced` 仅运行新增三条，`--group basic` 运行原四条；`--case` 优先选择单条。

### 本轮验证与 Badcase

- 使用真实 DeepSeek、本机 HTTP、隔离 SQLite 和合成业务数据；未触发内容生产、安装、重新调研或发布，未修改正式运营数据。本轮只修改评测，不修改 Runtime/业务策略。
- `change_mind`：通过，第二轮持久化预览只含 c1，新标题正确，角度和来源保留。5 次模型请求、3 次工具调用；input 15,367 / output 608 / cache hit 13,824，7.80 秒。
- `ambiguous_series`：初版两个空队列可能掩盖歧义问题，改为两个同名栏目各有一个真实隔离库选题后重新验证，通过工具/状态约束。人工检查回答确实列出两个账号及不同选题并询问账号。4 次模型请求、5 次工具调用；input 11,223 / output 558 / cache hit 10,112，7.25 秒。回答出现英文开头且对工具能力概括过窄，记录为表达质量问题，不宣称自然语言全文正确。
- `stale_batch`：未通过本例的严格路径约束。首轮读完后改变受众，次轮 Agent 直接调用 `prepare_topic_selection`，未再次 `get_topic_research`。服务端以 `invalid_request` 拒绝，未产生预览或入队；最终回答正确解释配置变化并请求用户决定是否重调研。4 次模型请求、2 次工具调用；input 11,197 / output 612 / cache hit 10,240，7.06 秒。
- 上述失败是“主动刷新状态”策略未满足，不是越权写入或错误产物：若任务指标仅要求不沿用过期候选，最终安全结果成立。当前 `passed` 同时包含路径约束，不能直接标为用户任务成功率；后续应拆开最终状态成功、路径效率和回答质量。GET 预检也无法替代服务端原子校验。
- 原三例证据：`tmp/studio-eval-20260911-020032/report.json`；加强歧义案例的单次重跑：`tmp/studio-eval-20260911-020330/report.json`。不将不同版本/不同运行拼成一次全量结果，不用重复尝试挑最高分。
- 基础四例回归 4/4：`tmp/studio-eval-20260911-020400/report.json`。本地确定性评分器 `python -m tests.smoke_studio_eval_grader` 通过；无前端修改，未做浏览器视觉验收。
- 下一步优先拆分评测维度，再决定是否要求模型预检旧候选：比较“直接尝试并处理拒绝”和“先查询再准备”的正确性与调用成本，避免仅为指定工具顺序而优化。仍不以当前小开发集宣传泛化指标。

## 范围（2026-09-11）

- 用户授权代写并用真实 DeepSeek 验证。复用 Web Agent Loop、真实本机 HTTP、SQLite 与业务工具，不另写 Agent。
- 四条开发集任务：查询栏目、选择第二条候选、改标题生成预览、反序批量预览。每条独立数据库/会话，固定候选明确为合成测试数据，不宣称真实调研。
- 选择任务以持久化 Preview 的候选身份、顺序、标题、角度及来源为成功依据；所有任务要求实际队列未写入。查询任务同时检查实际目录工具和回答中的栏目名称。
- 记录每条成功/失败、检查项、工具名称、原始消息路径、API usage 和耗时。没有 usage 时记 null，不估为零。第一次仅建立开发集基线，不报告泛化能力或优化增益。
- 生图、安装、重新调研为本集合禁止的动作，调用会被评测宿主拦截并计失败；不消耗 Codex 额度。不覆盖发布、恢复率或浏览器视觉。
- 入口：`python -m tests.eval_studio_tasks`；结果写入忽略目录 `tmp/studio-eval-<timestamp>/report.json`，逐条增量保存。支持 `--case` 单条运行。

## 验证结果

- 四条真实 DeepSeek 任务全部通过（4/4）；每条 2 次工具调用、3 次模型请求。累计 input 32,215 / output 1,212 / cache hit 28,672 tokens；输入是跨请求累加，缓存数包含于 input，不能重复相加。
- 目录查询 4.28 秒；单选 3.75 秒；改标题 5.03 秒；批量反序 3.47 秒。只报告本次运行，不能当成延迟分布或优化增益。
- 原始证据：`tmp/studio-eval-20260911-015604/report.json`；各子目录有隔离 SQLite、完整 Session、持久化 operation 和单题报告。
- 评测器增加本地反例验证：顺序错误、漏选、错误候选 ID、错误标题、丢来源及空 brief 必须失败。该测试仅验证确定性评分器，不替代真实模型。
- 下一批补多轮更改、指代歧义、旧候选和重复选择，并拆分开发集/保留测试集。当前没有恢复类案例，不报告 Recovery Rate；没有人工核验自然语言全文，不宣称答案完全正确。
