# CreatorOS Studio Web SPEC

## 信息架构重构实施（2026-09-25，完成）

- 落地"栏目 / Skill / Agent"三导航。栏目页（/）：左栏 账号→栏目树（未分配独立成组，"+"新建栏目/账号行内表单），右栏工作台：栏目头（名称/归属/绑定）、归属下拉（revision CAS）、手动直加选题（走 A 策略直入队接口）、选题库条目级 编辑/删除/上移/下移/生产/查看运行。
- Skill 页（/skills，/studio 兼容跳转）：Mind/Visualize 双栏拖拽组合 + 安装入口（GitHub 链接+角色）+ 归属账号可选；账号列撤除。
- 选题后端新端点：`PATCH /api/topics/{id}`（编辑，生产中拒绝）、`POST /api/topics/{id}/delete`（有生产记录拒绝）、`POST /api/series/{id}/reorder`（直写完整调序）；自然幂等不进 write_receipts。`smoke_topic_crud=passed`。
- 删除 TodayPage/CreatorsPage/CreatorDetailPage/SeriesPage/StudioSpacePage/RunsPage 列表；保留 /runs/{id} 验收页与运营抽屉；/series/{id} 301 到 /?series=。
- 缺陷记录：OperationDrawer.close/show 历史上会删 `series` 查询参数（旧结构 series 在路径里无害）；新结构 series 是查询参数，关抽屉会丢选择态——全量 e2e 抓到（topic-research 在全量失败、单跑通过），改为不再删除该参数并重跑原路径通过。
- 验证：Playwright e2e 4 passed（studio-workflow 重写为工作台流）；typecheck/build 通过；1440×900 与 390×844 截图检查首页/SKILL/Agent。

## 信息架构重构（2026-09-24，用户确认）

- 用户判断：今日/创作/账号三页内容重合、生产入口被埋。新导航只留三项：**栏目（首页）/ Skill / Agent**。
- 栏目页 = 生产控制台：左栏 账号→栏目 层级树（未分配独立成组，可拖入账号），右栏选题队列为主界面——条目级增删改查+拖拽调序+直接生产入口；调研候选同列表淡态展示、一键入队；Run 详情从条目状态进入，全局"运行"列表页删除。
- Skill 页 = Mind/Visualize 双栏 + 拖拽组合创建栏目（可选归属账号）+ 安装入口（GitHub 链接+角色）上移。
- 后端新缺口（本设计需要）：选题删除（有生产记录禁止删）、编辑、直写调序端点；均为自然幂等，不进 write_receipts。
- 删除旧页面：TodayPage、CreatorsPage、CreatorDetailPage、RunsPage 列表、StudioSpacePage 账号/未分配列；保留 /runs/{id} 验收详情页与运营指令抽屉。

## 去装饰文案 + 拖拽组合（2026-09-24，用户验收反馈）

- 用户设计原则："人家会自己探索，如果不好探索那是设计不好，不是解释性语言不够"。删除：页面副标题、安装提示、账号卡副字段、侧栏"本地 Studio / 本地执行 · 人工验收"、Agent 欢迎块说明文字、会话脚注"切换不会中止/草稿清空"、底部"离开页面不取消…"、顶栏"Codex 未就绪"、"运营指令 Ctrl K"文字（收成纯图标按钮，aria-label 保留，快捷键与功能不变）。
- Skill 库改为 Mind / Visualize 两栏（用户命名）；卡片只显示图标+名字，点击展开描述与"加入组合"按钮；端到端/未分类归入"其他"并禁用。
- 真实拖拽：pointer 手势（6px 阈值）+ 跟手 ghost + data-drop 命中检测；Skill 拖入对应槽（错类型不高亮）、未分配栏目拖上账号卡即分配（revision CAS），下拉分配保留为等价操作；Escape 取消。
- 创建按钮文案"创建栏目"；未分配行只显示选题数，不显示 revision。
- e2e 依赖核对："运营指令" dialog/button aria-label、"看看我的账号和栏目 ↗"、"从你已有的账号开始"保留，spec 无需改动。

## 视觉方向再改判：Apple 式极简（2026-09-24，用户确认）

- 用户评价旧版为"一坨屎"、暖纸档案改版为"屎上雕花"，明确要求推倒重做：Apple 官网式极简——纯白底、`#f5f5f7` 灰卡、近黑正文 `#1d1d1f`、单一蓝色动作 `#0071e3`、药丸按钮、大号紧排标题；删除一切装饰性文字（编号 eyebrow、彩色 chip、角色 tag），保留 CreatorOS 品牌标。
- 字体从 Manrope 换为 Inter Variable（更接近 SF Pro），保留 IBM Plex Mono 仅用于 ID/技术字段。
- 范围：全站（今日/创作/账号/栏目/运行/Run 详情/Agent/抽屉），不是只改创作空间；结构与逻辑不动，只重写视觉层（theme/styles/creative-studio/agent/studio-space 五个样式文件全量重写，选择器保持）。
- 功能性标识保留但弱化：状态 pill 改为灰字+彩色小点；栏目的"单 Skill/组合"用灰色纯文本，不用彩色 chip。
- 验证：typecheck/build 通过；Playwright e2e 4 passed；1440×900 与 390×844 截图检查今日/创作/Agent/栏目；无横向溢出。
- 缺陷记录：重写 creative-studio.css 时误删 `.queue-card/.run-row/.queue-head { min-width: 0 }` 收缩规则，移动端整页溢出被 e2e 溢出断言捕获，已补回并重跑原路径通过；`.studio-shell` 显式声明 canvas 背景（e2e 主题断言依赖）。

## 创作空间 P3-S1 设计说明（2026-09-23）

- 用户目标：一个"像主编排栏目"的真实创作空间：左 Skill 库（真实角色）、中组合器+未分配栏目、右账号及栏目；组合即可建栏目，分配即归属。
- 入口：新导航"创作"（/studio），不替换现有页面；栏目详情复用既有 /series/{id}。
- 视觉：preview 的暖纸档案风 + K3 宣传片前端感——暖纸底 #fafaf8、暖灰描边、雾紫（mind）/赤陶（production）/灰绿（账号）点缀、炭黑主按钮；Manrope 正文 + IBM Plex Mono 档案编号/标签（fontsource 自托管）；lucide 图标替换 Unicode 符号。
- S1 范围：路由骨架 + 三栏真实只读数据 + 组合创建/账号分配写链路（P2 API 已存在）；Skill 安装入栏（S3）、跟手拖拽（S4）后续；未接线入口明确禁用，不出现假按钮。
- 状态：空库显示真实空态与创建入口；加载/失败/409 分别展示；保存未成功不显示完成。
- Given/When/Then：① 空库打开 → 三栏空态+创建入口可见，无示例数据。② 选 mind+production 填名称 → 创建成功出现在未分配栏，刷新仍在。③ 下拉分配账号 → 栏目出现在账号下，revision 推进。④ 旧单 Skill 栏目显示"单 Skill"标识，不显示第二槽。⑤ 1440/390 截图无横向溢出。
- 验收：typecheck/build/e2e 全绿；截图用户确认后再扩散全站视觉。

## 视觉方向改判（2026-09-23）

- 用户明确：旧主站淡色卡片风格不合格；新参照 = GPT 制作的 `/studio-preview`（暖纸底 `#fafaf8`、暖灰描边、雾紫/赤陶/灰绿低饱和点缀、炭黑主按钮、Inter 字体）+ K3 宣传片中的前端视觉（科学档案感）。
- 2026-09-23 上午对旧浅色主题的精细化改写（theme.css/styles.css）已回滚，未提交；视觉重设计不单独迭代旧页面，合入 P3「原型接真实数据」时以 preview 视觉语言整体落地。
- 已安装 `@fontsource-variable/manrope`、`@fontsource/ibm-plex-mono`、`lucide-react`（用户授权放开安装），供 P3 视觉落地使用；当前代码尚未引用。
- 主线回到 docs/studio/composition/SPEC.md：P1 数据/安装契约 → P2 共用服务与 Tools → P3 原型接真实数据 → P4 双 Skill 生产溯源 → P5 Trace/Eval 展示。

## 创作空间交互原型（2026-09-21）

## 创作空间交互原型（2026-09-21）

- 入口 `/studio-preview`，独立于正式Layout和API。用户确认先验证Skill库→组合栏目→分配账号→本地选题，不改正式数据，不接模型、生图或安装。
- 三栏：Skill库、内容/制作组合与未分配栏目、账号。去装饰文案与空KPI；常驻“交互原型·示例数据”。拖拽与点击等价，创建用dialog，账号分配可撤回；独立localStorage键持久化，写入失败明确告知。
- 新增Skill仅建示例条目，明确未安装；组合未验证，不伪称可生产。暂不增加账号CRUD、生产或Agent接口。原系统入口保留。
- 验收：选择/移除/错类型与正确拖拽、创建/取消、分配/撤回、查看/添加选题、返回/刷新；空名称/无效GitHub地址、键盘与390px布局。UI原型不发业务API请求；构建与真实浏览器操作分开记录。
- 实现：独立 React 页面与作用域 CSS，四个示例 Skill、两个示例账号；只写 `creatoros.studio-preview.v1` 浏览器存储，不导入业务 API。整张栏目卡片可打开详情；制作禁用并标明未接服务。
- 浏览器实测通过：点击选择两个 Skill、Escape 取消且焦点返回创建按钮；创建 Agent 图解；正确 Skill 拖入、错误类型拒绝；栏目拖进知识实验室、下拉撤回；打开详情、添加选题、分配账号，刷新后再打开仍保留选题与归属。无效非 GitHub URL 保留表单且不新增；手机点击选择/移除后创建按钮正确禁用。
- 缺陷记录：嵌入浏览器原生 HTML drag 未触发 drop，改用有 6px 阈值的鼠标 Pointer 手势与捕获，原路径重跑成功，保留点击/下拉等价操作（触屏使用点击）。取消表单残留错误提示已清除；弹窗默认焦点修正到名称输入。
- 视觉：查看桌面1440×900主页面及390×844整页；窄屏实际 `innerWidth=390/clientWidth=375/scrollWidth=375`，无横向溢出。移动端按单列滚动，不宣称触屏拖拽已实现。
- `npm --prefix web run build`（含 TypeScript）通过；存在现有主包超过500kB警告，暂未拆包。`git diff --check`通过。新增 `web/e2e/studio-preview.spec.ts` 覆盖主路径、pointer拖拽、拒绝、刷新、错误清除、零API请求；本轮宿主浏览器限制下未启动独立 Playwright runner，以上验收来自浏览器工具真实操作，不声称自动回归已通过。后续可用 `npm --prefix web run e2e -- studio-preview.spec.ts` 运行。
- 待用户试用后接真实双 Skill 服务与 Agent 接口；有效链接安装、正式生产、账号CRUD、长列表性能、存储配额故障、完整键盘巡检尚未验收。本轮未调用模型、生图或发布，未改正式数据库。原型示例不代表已安装 Skill。

## 统一选题库页面（2026-09-14，完成）

- 栏目主列表复用 topic-library API，全部/待选/已入队、分页；取消重复的右侧待生产队列与折叠全部队列。
- 待选进入既有批次选择编辑面板（返回统一列表），复用编辑/调序/Preview；一次确认一个批次，不新建跨批次事务。已入队沿用 start/resume/查看 Run，研究仅保留表单和批次状态入口。
- 筛选/分页存 URL；刷新不写入，不自动研究/生产；错误提供重试，过期待选禁选；已停用栏目禁生产。编辑本页草稿切换/返回会丢失，明确提示。
- 验收：列表去重、筛选分页/刷新、待选转入批次编辑、返回、旧确认/生产回归、错误态、1440/390 截图。隔离库与受控资料，不消耗生图/模型额度。
- 实现 TopicLibrary 复用真实 topic-library 接口，research 控件增加 controlsOnly，旧选择面板独立编辑视图保留。确认/生产的 topics 缓存失效覆盖 library 子键，查询每3秒刷新；不改确认和生产 API。
- 完整 E2E 3 passed（36.8s），含 TypeScript 构建；确认前 API Topic 数为0、确认后2项且原ID不变，改标题/调序/刷新/返回通过。原 Agent 对话、生产/返工/审批路径通过。测试服务器专属 seed 路由写隔离资料，正式 app 不暴露此路由。
- 读取503恢复、21项分页/刷新、过期禁选使用受控响应；确认流程用真实隔离 HTTP/SQLite，不伪称本轮调用 Codex 或 DeepSeek。未修改正式运营数据。
- 查看1440×900待选列表及390×844已入队/选择编辑截图，无页面横向溢出；证据 web/test-results/topic-research-candidate-b-b54a2-its-separate-from-selection/。首次桌面截图捕获了loading，补列表数量断言后重新截图。
- 发现测试 seed POST 缺 JSON 请求头被服务端拒绝，补 data={} 与成功断言后重跑原路径，不放宽服务端校验。当前跨批次一键选择仍不支持；新调研入口折叠在列表底部，生图/发布不自动执行。

## Agent 会话侧栏与回复排版（完成，2026-09-14）

- 用户确认桌面会话列表放在聊天左侧，替换下拉框；聊天区使用剩余空间、正文控制阅读宽度，输入框缩小，不改后端/模型/会话协议。
- react-markdown 增加 GFM 表格支持，表格容器内部横向滚动；保持 skipHtml、安全 URL、不加载模型图片。工具状态改轻量行，代码保持可读。
- 桌面左栏提供新对话、最近会话、当前选中与加载/失败/空状态；手机可展开历史列表。切换沿用清除未发送草稿规则，进行中的后台任务不取消；只在提交HTTP尚未返回时禁切换。
- 验收：选会话/新建视图/刷新不POST；Markdown表格真实thead/tbody；桌面/390px无页面横向溢出，输入不遮正文；提交失败保留输入且不重试；中文组合输入不误发送。纯UI受控响应+故障注入，不消耗模型、生图或修改正式运营数据。
- 实现：Agent专属布局扩展页面宽度，232px会话侧栏替代下拉框；手机历史列表可展开/收起。正文阅读宽度约800px，输入框缩小，工具状态为轻量文本行，所有13个Web工具有中文标签。新增remark-gfm支持表格，保留skipHtml和URL安全转换，表格内部滚动不撑宽页面。
- 发现并修复：聊天页打开运营抽屉原本丢掉chat参数；现在保留当前会话，Escape关闭后仍显示原对话。切换会话仍沿用清空未发送草稿行为，并在侧栏明示；未改变后台任务或消息协议。
- 验证：改动前受控聊天截图位于`tmp/agent-ui/before-desktop.png`；新增`e2e/agent-layout.spec.ts`操作覆盖切换/新对话/刷新零POST、抽屉返回同会话、表格/代码/HTML屏蔽、中文composition、Shift+Enter换行、503失败保留草稿且单次POST、手机展开历史与表格横向滚动。
- 构建含TypeScript检查通过；完整`npm --prefix web run e2e`为3 passed（49.9s），原创建/预览确认冲突/受控生产审批以及候选编辑流程未回退。已查看1440×900桌面、390×844手机和提交错误截图，位于`web/test-results/agent-layout-agent-history-e8821--and-composer-remain-usable/`。
- 本轮为UI受控响应与故障注入，不声称重新验证真实DeepSeek流式服务；原SSE/轮询、请求去重和业务API不变。无正式运营数据修改、模型调用或生图；npm audit此次安装报告0 vulnerabilities。

## 全站方向 B（2026-09-09，完成）

- 将已确认的浅色创意索引扩展到首页、账号、栏目、运行、产物详情、Agent 和确认抽屉；用共用颜色变量维护背景、正文、边界、操作和状态色。
- 保留现有 API、草稿、Preview 确认和后台生产流程；本轮集中统一视觉，栏目页维持现有内容结构。
- 验收：跨页面导航保持浅色；创建账号/栏目后刷新可见；Preview/冲突提示与确认可读；产物翻页、返工、批准路径通过；Agent 输入、代码和工具结果可读；桌面与 390px 检查。
- 使用隔离数据库及现有受控生产 E2E，避免布局验证触发生图；另检查 Agent 空态/已有消息及错误反馈。正式运营数据不写入。
- 实现：原暗色 CSS 的颜色在原定义中迁移到 theme.css 语义变量；栏目主题提升为全站共用，统一卡片、表单、Agent 输入/工具消息、图片验收与 dialog。顶部改为当前页面名称，账号与概览使用小面积多色标识。
- 修复：手机首页长运营计划与运行列表长标题撑宽 grid；设置可收缩列宽并逐页检查越界元素。手机栏目标题与 Skill 按钮改为换行布局。Agent 发送按钮使用正确的共用主按钮样式。
- 验证：构建通过；Playwright 两条 E2E 通过（38.2s），覆盖创建、确认冲突、受控生产/返工/批准、刷新、候选编辑重选调序，以及首页/账号/运行/Agent 的 1440×900 与 390×844 浅色和溢出断言。Agent 预填消息、打开抽屉/Escape 返回、503 故障提示实际操作通过。
- 已看图：首页空态/有数据、账号目录、栏目与候选、运行、产物验收、确认冲突抽屉、Agent 桌面/手机和错误态。截图在 web/test-results；没有新增生图或真实模型调用，本轮未重新验证模型生成和已有对话流式内容。

## 方向 B 栏目页（已完成，2026-09-09）

- 用户选定浅色创意索引：中性背景、石墨文字、蓝色操作和蓝/橙/绿几何主题；共用侧栏更新，其他业务页暂不重做。
- 用户确认：右侧只显示待生产项且可开始生产；候选勾选不展开编辑，点击编辑才展开，保存后收起。
- 数据与 API 不改：候选批次 → Preview → 人工确认；生产继续原 ContentRun。栏目设置入口仅管理已实现的 Skill，不摆假设置按钮。完整队列保留所有状态与已有恢复入口。
- 选择和编辑草稿留当前页面，切换批次清空；取消编辑不影响已保存草稿；重新全选不能覆盖用户已改标题/切入点。生产提交成功后显示对应 Run 链接。
- 验收：勾选不展开；编辑保存/取消、全选/取消/调序、来源；Preview 零入队、确认后顺序正确、刷新不重复；错误/过期禁写；桌面和手机真实截图。E2E 用隔离库与受控研究/生产，不消耗真实生图额度，不改正式数据。
- 实现：栏目页采用浅色纸面布局、低饱和蓝/橙/绿几何标记；右侧固定待生产队列，显示未完成项并提供开始/恢复生产；完整队列、手动选题和生产 Skill 均折叠到次级入口。候选卡片保持浏览密度，只有点击“编辑”才展开本页草稿表单，草稿在取消勾选后仍保留，确认前不写入后端。
- 验证：`npm run typecheck`、`npm run build` 通过；Playwright 隔离服务覆盖原生产/审批流程及候选选择、编辑保存、取消重选、入队调序（2 passed）；在隔离调研数据库的本地 Studio 中完成栏目页桌面截图检查，正式数据库与真实生图额度未触碰。移动宽度由 Playwright 390px 回归检查无横向溢出。

## 前端工作规范（2026-09-09）

- 用户暂停业务推进，要求先建立能发现操作问题并自我迭代的前端工作流。新增 `FRONTEND_WORKFLOW.md`，根 AGENTS 强制引用；本轮不重做页面、不迁移框架、不安装工具、不生成素材。
- 保留 React/TS/Vite，区分设计、浏览器探索、诊断与自动回归；后续以真实操作及 UI/API 状态双证据验收，截图只证明外观。
- 已核查 package.json、playwright.config.ts 与既有 E2E：有一条集成流程和失败 trace/screenshot；没有截图 diff 基线，调研候选尚未纳入自动 E2E。上述缺口记录为待做，不声称文档创建即完成测试建设。
- 本轮文档验收：所引用仓库路径、npm script 和 Python 路径检查；Git diff 检查。不运行 UI/API 验收，因为没有修改运行代码。
- 下一步先确定视觉方向和一个代表页面，再按新规范做栏目页切片并补候选选择 E2E；不先整站重写。

## 调研候选面板（2026-09-09）

- 栏目页新增数量/偏好调研入口、持久化批次选择、候选勾选/标题/切入点修改与入队顺序；共用原 Preview 确认抽屉，不另建队列执行器。
- 切换批次清空未确认选择，已入队禁选、过期禁提交；断刷新不重新调研。390px 和真实候选确认验收通过，见 docs/agent-studio/topic-research/SPEC.md。

## Web Agent 页面（2026-09-08）

- 新增 /agent 页面与导航；最近对话选择、连续追问、文字/工具状态、Run 链接、用量折叠。原 Ctrl+K Preview 抽屉保留；不把自然语言生产伪装成选题 Parser。
- POST 不重试；SSE 快照替换而不拼接、断线轮询兜底；刷新/路由切换不触发模型。服务端拒绝过期或并发指令；未知结果提示先查询。
- react-markdown 渲染正文，不开启原始 HTML，不自动加载模型输出的图片；Run 链接转同源页面，其余链接保留默认 URL 安全转换。
- 验证与后续边界见 `docs/agent-studio/web-chat/SPEC.md`。

## 当前理解

- React + TypeScript + Vite 是本地 Studio 的展示层；Python/FastAPI 仍是业务查询与状态真相。
- S2 只读消费 `/api/overview`、`/api/creators`、`/api/series/*`、`/api/runs`，不直接连接 SQLite，也不调用 LLM、PersonClone 或 Codex。
- 产品主入口是“今天 / 账号 / 运行”。栏目和选题在账号详情、栏目详情中展开，避免首页铺满历史队列。

## 当前实现（S2–S7）

- 建立可运行的 Web Studio 外壳：响应式侧栏、真实 API client、React Router 页面和 TanStack Query 服务器状态。
- 首页、账号目录、账号详情、栏目选题、运行列表/只读详情都能用真实 DTO 渲染；空库、加载和 API 错误都有清晰下一步。
- S2 先把“看见现在有什么、下一步能做什么”做对；S3 再接通账号、栏目和选题的真实写入，但仍不接生产、图片验收或批准。
- S4 从栏目选题和 Run 详情显式开始/恢复生产，显示真实状态与运行时长；可离开页面继续管理选题。

## 设计决策

- S2–S3 采用一份轻量 `styles.css`，而不是现在就搭 Tailwind/shadcn 设计系统：页面数量少、视觉规则集中，先控制依赖和可读性；出现重复组件后再抽象。
- Vite 开发服务器把 `/api` 代理到 `127.0.0.1:8765`，因此浏览器不会接触数据库或处理跨域写权限；`VITE_API_BASE_URL` 可用于同源部署以外的只读地址。
- 组件只展示后端给出的状态/允许动作；S3 的写按钮调用真实 API，创建选题必须先 Preview 再确认。不写入演示账号，不把空库填成假数据。
- 首屏不放固定 KPI、巨大 ASCII Logo 或霓虹渐变；数字均来自 `OverviewView.counts`，状态同时显示文字和低饱和色点。

## 对外影响

- 新增 `web/` 前端工程与 `web/package-lock.json`；生产构建产物和 `node_modules` 忽略。
- README 增加双终端启动方式：FastAPI 在 8765，Vite 在 5173。
- 后端 S1 查询投影保持稳定；S3 新增最小写路由，仍不启动模型、PersonClone、Codex 或生产 Worker。

## 验收

- `npm run typecheck`、`npm run build` 通过。
- 正式空数据库显示首次使用引导，不显示虚假 Creator；服务不可达显示错误与重试。
- 以隔离临时 SQLite 启动同一 API，页面能显示真实测试账号/栏目/选题布局；测试数据不进入正式库。
- 在 1440×900 和 390px 宽检查首屏层级、无横向溢出、详情可回退；浏览器刷新详情 URL 仍由前端路由承接。

## 最近验证

- 2026-09-03：`npm install` 安装 31 个前端依赖，`npm run typecheck` 与 `npm run build` 通过（Vite 8.2.2，产物约 284 kB JS / 13.4 kB CSS）。
- 2026-09-03：启动真实 `python -m creatoros.web` + Vite，浏览器访问 `/`；正式空库显示首次使用引导，未出现假账号。
- 2026-09-03：以 `tmp/studio-demo.db` 隔离 SQLite 启动同一 API，浏览器检查首页、账号目录、账号详情、栏目选题详情；真实 Creator/Series/Topic 投影可见，页面 DOM `scrollWidth === innerWidth`（415px 移动视口），无控制台 warning/error。
- 2026-09-03：S2 未调用 LLM、PersonClone、Codex、生图或发布 API；临时演示数据未进入 `data/creatoros.db`。
- 2026-09-03：S3 接口 smoke 覆盖创建账号/栏目、Preview 零写入、确认写入、版本冲突和重复确认幂等；浏览器在隔离 SQLite 中完整走通账号 → 栏目 → 两个选题 → Preview → 确认。
- 2026-09-04：S4 以服务端固定幂等键创建 Run，再带所见版本执行；busy/网络异常后查询现有状态，不自动重发生产。详情显示生产时长、heartbeat 超期提示和返回栏目入口。
- 2026-09-04：隔离浏览器走通生产期间跨栏目新增/确认选题、busy 链接、有序重启与显式恢复。检查 1440×900 和 390px 截图，修复详情卡片的横向溢出；未把测试账号写入正式库。
- 2026-09-04：npm typecheck/build 通过，新增 RunControls；活跃任务约 2 秒、闲置约 10 秒轮询。图片预览/批准/返工尚未开放。

## S5 实现与验证（2026-09-04）

- 图片占详情主体，右侧为发布文案和批准/返工；缩略图、前后切换、放大及旧版本查看，保留原图比例。
- 旧版本醒目标注并禁用写动作；返工提交不自动生产；批准明确为“尚未发布”。技术 Attempt/usage 与事件记录折叠。
- SSE 只触发查询刷新，按事件 ID 去重；失败显示重连并继续轮询。冲突必须由用户重新检查，不能拿刷新后的版本自动重试。
- 验收：真实本地 API、已有图片及隔离测试库；桌面/390px 浏览器检查，无横向溢出，页面刷新和历史切换不触发生产。
- `RunInspector` 以真实图片为主，保留原始宽高比；1～N 张缩略图、键盘前后切换、原图放大、复制文案与安全来源链接。旧版隐藏写动作，409 后展示重新检查入口。
- SSE `snapshot/run_event` 触发查询刷新，按持久化事件 ID 合并去重；轮询仍是故障兜底。关闭详情仅结束 EventSource，不操作生产。
- 已看图检查 1440×900、390×844；修正手机端图片容器多余留白。真实已有 5 张和 6 张图片都可翻阅；浏览器走通旧版无批准按钮、返工第 3 版停在待开始、复制文案、批准后刷新仍为已批准。
- 测试账号明确标记隔离测试，使用临时数据库及原图副本；不把样例导入正式库、不声称本轮调用生图。

## 下一步

- S6：自然语言选题计划入口；S7：完整演示与交付。保持前端服务于生产/验收，不继续无限装修。
## S6 完成（2026-09-04）

- Layout 共用原生 dialog：顶部按钮/Ctrl或Cmd+K、栏目“用一句话调整”、表单及首页待办均可进入。URL 保存 operation ID，分页入口可找回旧待处理计划。
- 草稿关闭重开保留；澄清/unsupported/stale 走同计划 edit。显示新增、前后顺序和最终队列，技术版本折叠。
- 用户所见快照不随 refetch 静默替换，版本变化禁用确认；晚到结果只显示就绪通知，不重开/覆盖其他页面。首页同时显示失败 Run 与计划。
- 浏览器验证真实新增、编辑、确认、刷新、双标签冲突、Enter、Esc/焦点返回及延迟完成通知；1440×900 与 390×844 已看图，修复边缘裁切/双滚动条。
- typecheck/build 通过，浏览器 error 日志为空；截图位于忽略的 tmp/s6_final_qa。IME 有 composition 保护，但未自动模拟真实输入法候选提交。

## S7 完成（2026-09-05）

- `python -m creatoros.web` 自动检查 Vite build，同源托管 Studio/API；React 详情 URL 直接刷新、Windows JS MIME、本机自定义端口和有序关闭均已验证。
- 健康状态会区分本地就绪、表单模式、Codex 未就绪与 API 失联；README 提供一个日常启动命令及 2–3 分钟演示路径。
- Playwright/Chrome E2E 在全新隔离库覆盖创建、Preview、过期确认、生产、返工、批准、刷新和 390px 布局；受控生产器只作产品测试。
- 独立真实 Codex Run 暴露部分回执挂起后，完成 `interrupted → resume → awaiting_approval` 恢复；最终 7 张真实图片经 API 与浏览器 Inspector 逐张验证，未发布。详细记录见 `docs/studio/s7/SPEC.md`。
## 栏目生产 Skill 面板（2026-09-08）

- 栏目页增加折叠式安装/绑定入口；任务 ID 在 URL 保留，读状态不触发模型。选择版本后显示旧/新绑定，用户显式确认，提交携带选择时所见值，拒绝自动重试冲突。390px 与刷新持久化已验收，见 docs/agent-studio/producer-skills/SPEC.md。
